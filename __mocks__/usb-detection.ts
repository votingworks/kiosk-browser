const startMonitoring = jest.fn()
const stopMonitoring = jest.fn()
const find = jest.fn()
const on = jest.fn()
const off = jest.fn()

export default {
  startMonitoring,
  stopMonitoring,
  find,
  on,
  off,
}
